import classes from './Layout.module.css';

const Header = () => {
  return (
    <footer className={classes.footer}>
      <label>Francisco Segarra - 2022</label>
    </footer>
  )
}

export default Header;